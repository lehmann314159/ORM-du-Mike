CREATE TABLE TestTable
(
	id    INT,
	name  VARCHAR(20),
	quest VARCHAR(80),
	color VARCHAR(20)
);

INSERT
	INTO TestTable ( id, name, quest, color )
	VALUES ( 1, "Sir Mike", "To Find the Grail", "I don't Know" );
